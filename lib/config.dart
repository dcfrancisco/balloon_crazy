// config.dart
const gameWidth = 820.0;
const gameHeight = 1600.0;
const balloonRadius = gameWidth * 0.5;

// Spacing and layout adjustments
const topSpacing =
    50.0; // Adjust the distance from the top to the first row of balloons
const horizontalSpacing = 20.0; // Horizontal spacing between balloons
const verticalSpacing = 20.0;  // Vertical spacing between rows
